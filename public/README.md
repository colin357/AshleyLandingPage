# /public

Replace `ashley.jpg` with the real photo of Ashley Hickmon.

Recommended: a portrait-orientation JPEG or PNG, roughly 720×900 px or
larger. The Hero component references `/ashley.jpg` by path — keep the
filename identical, or update `components/Hero.tsx` if you change it.
